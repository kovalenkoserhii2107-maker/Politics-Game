{
  "name": "politics-game",
  "version": "0.3.0",
  "private": true,
  "description": "Mobile-first political and economic world strategy",
  "scripts": { "test": "node --test tests/*.test.cjs" },
  "engines": { "node": ">=20" }
}
